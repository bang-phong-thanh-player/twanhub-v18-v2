// supabase.js
// Tạm thời chỉ là placeholder, sau này mình gắn SDK thật.

window.TwanSupabase = (function () {
  function init() {
    console.log(
      "Supabase placeholder init - sau này sẽ gắn auth & sync notes / cloud."
    );
  }

  return { init };
})();
